name: 1-Feature

on:
  push:
    branches:
      - feat**  # Aciona o workflow em branches que começam com "feat"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore src/app/authcloud.csproj

      - name: Build application
        run: dotnet build src/app/authcloud.csproj --configuration Release --no-restore

      - name: Run tests and collect coverage
        run: dotnet test --configuration Release /p:CollectCoverage=true /p:CoverletOutputFormat=opencover /p:CoverletOutput=coverage/coverage.opencover.xml

      - name: Install report generator tool
        run: dotnet tool install --global dotnet-reportgenerator-globaltool

      - name: Publish coverage results
        run: reportgenerator -reports:unit-tests/coverage/coverage.opencover.xml -targetdir:coverage_report -reporttypes:Html
        env:
          DOTNET_CLI_TELEMETRY_OPTOUT: 1

      - name: Upload coverage results
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage_report
